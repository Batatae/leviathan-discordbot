from discord.ext import commands

class COMANDO(commands.Cog):
    def __init__(self, bot):
        self.bot = bot
        
    @commands.command(help="DESCREVER O COMANDO BRVEMENTE")
    #codigo

async def setup(bot):
    await bot.add_cog(COMANDO(bot))
